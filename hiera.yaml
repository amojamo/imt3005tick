---
version: 5

defaults:
  datadir: "data"

hierarchy:
  - name: 'Yaml backend'
    data_hash: yaml_data
    paths:
      - name: "Common data"
        path: 'common.yaml'

      - name: "Per-node data"
        path: "nodes/%{trusted.certname}.yaml"

